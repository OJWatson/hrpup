% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R, R/who_mapping.R
\name{scaleBar}
\alias{scaleBar}
\title{Add a Custom Position Scale Bar to ggplot}
\usage{
scaleBar(
  lon,
  lat,
  distanceLon,
  distanceLat,
  distanceLegend,
  dist.unit = "km",
  rec.fill = "white",
  rec.colour = "black",
  rec2.fill = "black",
  rec2.colour = "black",
  legend.colour = "black",
  legend.size = 3,
  box = TRUE,
  box.line.color = "black",
  box.fill.color = "white",
  box.offset = 1
)

scaleBar(
  lon,
  lat,
  distanceLon,
  distanceLat,
  distanceLegend,
  dist.unit = "km",
  rec.fill = "white",
  rec.colour = "black",
  rec2.fill = "black",
  rec2.colour = "black",
  legend.colour = "black",
  legend.size = 3,
  box = TRUE,
  box.line.color = "black",
  box.fill.color = "white",
  box.offset = 1
)
}
\arguments{
\item{lon}{Numeric. Longitude of the bottom-left point of the first rectangle.}

\item{lat}{Numeric. Latitude of the bottom-left point of the first rectangle.}

\item{distanceLon}{Numeric. Length of each rectangle (distance along longitude).}

\item{distanceLat}{Numeric. Width of each rectangle (distance along latitude).}

\item{distanceLegend}{Numeric. Distance between rectangles and legend texts.}

\item{dist.unit}{Character. Units of distance: \code{"km"} (default, kilometres), \code{"nm"} (nautical miles), or \code{"mi"} (statute miles).}

\item{rec.fill}{Character. Fill colour of the first rectangle (default: \code{"white"}).}

\item{rec.colour}{Character. Border colour of the first rectangle (default: \code{"black"}).}

\item{rec2.fill}{Character. Fill colour of the second rectangle (default: \code{"black"}).}

\item{rec2.colour}{Character. Border colour of the second rectangle (default: \code{"black"}).}

\item{legend.colour}{Character. Colour of the legend text (default: \code{"black"}).}

\item{legend.size}{Numeric. Font size of the legend text (default: \code{3}).}

\item{box}{Logical. If \code{TRUE} (default), adds a background box for better visibility.}

\item{box.line.color}{Character. Colour of the box border (default: \code{"black"}).}

\item{box.fill.color}{Character. Fill colour of the box (default: \code{"white"}).}

\item{box.offset}{Numeric. Offset distance of the box from the scale bar (default: \code{1}).}

\item{orientation}{Logical. If \code{TRUE} (default), adds an orientation arrow.}

\item{arrow.length}{Numeric. Length of the arrow in the same units as \code{dist.unit} (default: \code{500}).}

\item{arrow.distance}{Numeric. Distance between the scale bar and the bottom of the arrow (default: \code{300}).}

\item{arrow.North.size}{Numeric. Font size of the "N" label on the arrow (default: \code{6}).}

\item{arrow.color}{Character. Colour of the orientation arrow and "N" label (default: \code{"black"}).}
}
\value{
A list of \code{ggplot} layers that include:
\itemize{
\item Rectangles for the scale bar.
\item Legend text for the scale bar.
}

A list of \code{ggplot} layers that include:
\itemize{
\item Rectangles for the scale bar.
\item Legend text for the scale bar.
}
}
\description{
This function enables drawing a scale bar on a \code{ggplot} object and optionally adding an orientation arrow.

This function enables drawing a scale bar on a \code{ggplot} object and optionally adding an orientation arrow.
}
\examples{
# Add a scale bar to a ggplot map

\dontrun{
ggplot(data = map_data, aes(x = lon, y = lat)) +
  geom_polygon() +
  scaleBar(lon = 0, lat = 0, distanceLon = 100, distanceLat = 10, distanceLegend = 15)
}

# Add a scale bar to a ggplot map

\dontrun{
ggplot(data = map_data, aes(x = lon, y = lat)) +
  geom_polygon() +
  scaleBar(lon = 0, lat = 0, distanceLon = 100, distanceLat = 10, distanceLegend = 15)
}

}
